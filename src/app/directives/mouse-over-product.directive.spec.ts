import { MouseOverProductDirective } from './mouse-over-product.directive';

describe('MouseOverProductDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseOverProductDirective();
    expect(directive).toBeTruthy();
  });
});
